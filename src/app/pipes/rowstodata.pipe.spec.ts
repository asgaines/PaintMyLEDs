import { RowstodataPipe } from './rowstodata.pipe';

describe('RowstodataPipe', () => {
  it('create an instance', () => {
    const pipe = new RowstodataPipe();
    expect(pipe).toBeTruthy();
  });
});
